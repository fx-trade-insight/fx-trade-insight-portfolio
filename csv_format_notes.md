# FX口座別 CSVフォーマット仕様メモ

各FX口座のCSV取り込み仕様をまとめる。
新しい口座のパーサーを実装する際の参考資料として使用すること。

> **更新ルール**: 新しい口座のパーサーを追加・変更した場合は、このファイルを必ず更新すること。
> - 「実装済み口座」セクションに仕様を追記する
> - 「未実装口座」セクションから実装済みの口座を削除する
> - レコードIDのプレフィックス一覧表を更新する

---

## 共通仕様

- CSVはフォルダ構成 `{CSV格納先}/{口座名}/ファイル名.csv` で配置する
- 取り込み後、処理済みCSVはフォルダから自動削除される
- 重複チェックはレコードIDで行い、既存IDは `skipped` としてカウント

### スワップポリシー

**全口座共通: `soneki` はスワップを含まない純トレード損益を格納する。**

スワップはトレードの技術（エントリー・エグジット判断）とは無関係な要因（保有日数・金利差）によるため、
成績集計の `soneki` から除外する。スワップ込みの合算列が存在する場合も意図的に使用しない。

| 口座 | 使用列（スワップ除外） | スワップ列 |
|------|----------------------|-----------|
| GMOクリック証券 | `実現損益（円貨）` | CSV出力時に除外（手順で「取引」のみチェック） |
| DMM FX | `建玉損益（円）` | `スワップ`（列6）が別列 |
| JFX/ヒロセ通商（アプリ） | `売買損益` | `スワップ損益`（列18）が別列 |
| ヒロセ通商（Web） | `売買損益`（列11） | `スワップ損益`（列13）が別列 |
| 外為どっとコム | `スポット損益` | `スワップ損益`が別列 |
| 楽天 FX | `取引損益(円換算)` | 公式ヘルプで「売買損益の円換算値」と確認済み |
| 松井証券 | `建玉損益(円)` | `受渡金額`（建玉損益+スワップ合算）は使用しない |
| FXTF | `実現損益` | `受渡代金`（実現損益+実現スワップ合算）は使用しない |
| みんなのFX | `建玉損益`（WEB列5 / FX列7） | `累計スワップ`（WEB列6 / FX列8）が別列。`決済損益`は合算値のため使用しない |
| LIGHT FX | `建玉損益合計`（シンプル列5 / アドバンスド列7） | みんなのFXと同一フォーマット。`累計スワップ`は別列で除外 |
| MT4 | `Profit`列（cells[13]） | `Swap`列（cells[12]）が別列。Profitは純トレード損益のみ |
| OANDA証券（MT5） | `合計 - スワップ`（M列 - L列） | L列（スワップ）が別列。M列（合計）はスワップ込みのため差し引く |
| SBI FX | `実現損益` | `ｽﾜｯﾌﾟﾎﾟｲﾝﾄ`（列12）が別列 |

### レコードID統一フォーマット

DBの `id` カラムは **`{口座プレフィックス}_{新規番号}_{決済番号}`** の形式で統一する。
プレフィックスにより口座間のID衝突を防ぎ、ログや調査時に口座を即座に特定できる。

| 口座 | プレフィックス | 形式 | 例 |
|------|--------------|------|----|
| GMOクリック証券 | `gmo_` | `gmo_{新規約定番号}_{決済約定番号}` | `gmo_12345678_87654321` |
| DMM FX | `dmm_` | `dmm_{新規注文番号}_{決済注文番号}` | `dmm_111000104112095_111000104112184` |
| JFX | `jfx_` | `jfx_{注文番号}_{ポジション番号}` | `jfx_2505900202617907_2505900069943907` |
| ヒロセ通商 | `hirose_` | `hirose_{注文番号}_{ポジション番号}` | `hirose_2512900188224828_2512900065511228` |
| 外為どっとコム | `gaitame_` | `gaitame_{注文番号}_{ポジション番号}` | `gaitame_2412668100020317_2412683900013099` |
| 楽天 FX | `rakuten_` | `rakuten_{新規約定NO}_{決済約定NO}` | `rakuten_1_3` |
| 松井証券 | `matsui_` | `matsui_{通貨ペア}_{新規約定日時}_{決済約定日時}_{数量}_{新規価格×1000}_{決済価格×1000}` | `matsui_USD/JPY_20260518164351_20260518164401_1000_158857_158858` |
| FXTF | `fxtf_` | `fxtf_{決済対象取引番号}_{取引番号}` | `fxtf_3376086614664085504_3376086789080023040` |
| 外貨ex | `gaikaex_` | `gaikaex_{新規注文番号}_{決済注文番号}` | `gaikaex_11452161603_11452162075` |
| LINE FX | `linefx_` | `linefx_{新規取引番号}_{決済取引番号}` | `linefx_2613900003619002_2613900003620202` |
| みんなのFX | `minnafx_` | `minnafx_{新規約定番号}_{決済約定番号}` | `minnafx_2748256411135586846_2748257076872695326` |
| LIGHT FX | `lightfx_` | `lightfx_{新規約定番号}_{決済約定番号}` | `lightfx_2748665121837085214_2748666045465599006` |
| MT4（FXTF・楽天 FXなど） | `mt4_` | `mt4_{チケット番号}` | `mt4_113398869` |
| OANDA証券（MT5） | `oanda_` | `oanda_{ポジション番号}` | `oanda_39021811` |
| SBI FX | `sbi_` | `sbi_{新規注文番号}_{決済注文番号}` | `sbi_1234567890_1234567891` |

新しい口座のパーサーを追加する際は、必ず固有のプレフィックスを設けること。

### Lot単位について

DBの `lot` カラムは**通貨数（実際の取引通貨量）**で統一して格納する。
口座によってCSVの単位が異なるため、パーサーで変換してから登録する。

| 口座 | CSVの単位 | CSV値の例 | DB格納値 | 変換処理 |
|------|----------|----------|---------|---------|
| GMOクリック証券 | 通貨数（そのまま） | 200000 | 200000 | 変換なし |
| DMM FX（通常） | Lot数（1 Lot = 1万通貨） | 1 | 10000 | × 10,000 |
| DMM FX（ミニ: USM/JPY等） | Lot数（1 Lot = 1,000通貨） | 1 | 1000 | × 1,000 |
| JFX | Lot数（1 Lot = 1,000通貨） | 120 | 120000 | × 1,000 |
| ヒロセ通商 | Lot数（1 Lot = 1,000通貨） | 100 | 100000 | × 1,000 |
| 外為どっとコム | Lot数（1 Lot = 1,000通貨） | 20 | 20000 | × 1,000 |
| 楽天 FX | 通貨数（そのまま） | 1,000 | 1000 | カンマ除去のみ |
| 松井証券 | 通貨数（そのまま） | 1,000 | 1000 | カンマ除去のみ |
| FXTF | 通貨数（そのまま） | 1,000 | 1000 | カンマ除去のみ |
| 外貨ex | 通貨数（そのまま） | 1000 | 1000 | 変換なし |
| LINE FX | 万通貨単位（"数量x10,000" 列） | 0.1 | 1000 | × 10,000 |
| みんなのFX | 万通貨単位（"数量" 列） | 0.1 | 1000 | × 10,000 |
| LIGHT FX | 万通貨単位（"数量" 列） | 0.1 | 1000 | × 10,000 |
| MT4 | Lot数（1 Lot = 100,000通貨） | 0.01 | 1000 | × 100,000 |
| OANDA証券（MT5） | Lot数（1 Lot = 100,000通貨） | 0.01 | 1000 | × 100,000 |
| SBI FX | 通貨数（そのまま） | 1000 | 1000 | 変換なし |

新しい口座を実装する際はCSVの単位を確認し、通貨数に換算して登録すること。

---

## 実装済み口座

### GMOクリック証券

| 項目 | 内容 |
|------|------|
| **文字コード** | Shift-JIS（CP932） |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル並び順** | 不定（ソート処理あり） |
| **パーサー** | `src/main/parsers/gmo.ts` |

**カラム構成（参照列のみ）:**

| カラム名 | 用途 |
|----------|------|
| 約定日時 | エントリー/決済の日時 |
| 取引区分 | 「新規」または「決済」 |
| 銘柄名 | 通貨ペア（例: `USD/JPY`） |
| 売買区分 | 「買」または「売」（その取引の方向） |
| 約定数量 | Lot数 |
| 約定単価 | 約定価格 |
| 実現損益（円貨）/ 実現損益（円換算額） | 決済行の損益（円）。列名はCSVバージョンにより異なる |
| 約定番号 | 注文ID（重複チェックと新規/決済ペアリングに使用） |
| 建単価 | 新規時の約定価格。決済行にも記載され、ペアリング精度向上に使用 |

**ペアリングロジック:**
- 新規行を `{銘柄名}_{売買区分}` キーのキューに積む
- 決済行が来たとき、`売買区分` の逆方向（例: 売→買）のキューを検索
- `建単価` で一致する新規行を優先照合し、なければFIFO
- レコードID: `{新規約定番号}_{決済約定番号}`

**スワップの扱い:**
- エクスポート時に「取引」チェックのみで出力する手順とする（スワップチェックは外す）
- これにより CSV にスワップ行は含まれない
- `実現損益（円貨）` はスワップを含まないトレード損益のみとなる（仕様）
- パーサーは `取引区分 === '新規' or '決済'` の完全一致で行を絞り込むため、万が一スワップ行が混入しても誤取込は発生しない

---

### DMM FX

| 項目 | 内容 |
|------|------|
| **文字コード** | CP932（Shift-JIS互換） |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル並び順** | 新しい順（降順）→ パーサー内で昇順ソート |
| **パーサー** | `src/main/parsers/dmm.ts` |

**カラム構成（全列）:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 通貨ペア | 通貨ペア（例: `USD/JPY`） |
| 1 | 売買 | 「買」または「売」（その取引の方向） |
| 2 | 区分 | 「新規」または「決済」 |
| 3 | 数量（Lot） | Lot数 |
| 4 | 約定レート | 約定価格 |
| 5 | 建玉損益（円） | ポジション単体の損益 |
| 6 | スワップ | スワップポイント（円） |
| 7 | 決済損益（円） | 建玉損益 + スワップ の合算値（取り込みには使用しない） |
| 8 | 注文日時 | 注文受付日時（未使用） |
| 9 | 約定日時 | エントリー/決済の日時（ペアリング・ソートに使用） |
| 10 | 注文番号 | 注文ID（重複チェックとペアリングに使用） |
| 11 | 円転レート | 外貨建て損益の円換算レート（未使用） |
| 12 | 取引手数料 | 手数料（未使用） |
| 13 | 建玉損益 | ¥プレフィックス付き損益表示（未使用） |

**ペアリングロジック:**
- GMO同様、新規/決済の2行ペア方式
- `建単価` 列がないため、**純粋なFIFO照合のみ**
- ファイルが降順のため、昇順ソート後にキューを構築する
- レコードID: `dmm_{新規注文番号}_{決済注文番号}`

**スワップの扱い:**
- `スワップ`（列6）が別列で管理されており、`決済損益（円）`（列7）= 建玉損益 + スワップ の合算値
- パーサーは **`建玉損益（円）`（列5）を使用**し、スワップを除外した純トレード損益のみ取り込む
- `区分` の完全一致フィルタにより、スワップ行（`区分 = 'スワップ'` 等）が混入しても誤取込は発生しない

**ミニ通貨ペア対応:**

DMM FXでは「ミニ」口座で1,000通貨単位（1 Lot = 1,000通貨）の取引が可能。ミニ通貨ペアはCSVでの表記と変換先が以下の通り。

| CSV表記 | DB格納値 | 1 Lot |
|---------|---------|-------|
| `USM/JPY` | `USD/JPY` | 1,000通貨 |
| `EUM/JPY` | `EUR/JPY` | 1,000通貨 |
| `GBM/JPY` | `GBP/JPY` | 1,000通貨 |
| `AUM/JPY` | `AUD/JPY` | 1,000通貨 |

パーサーはミニ通貨ペアを `MINI_SYMBOL_MAP` で検出し、通貨ペア名を標準形式に変換しつつ、lot計算に `MINI_LOT_UNITS`（1,000）を使用する。

**GMOとの主な違い:**
- 建単価による精度の高いペアリングができない（FIFO固定）
- スワップが別列で管理されており、`決済損益（円）` が合算値だが、取り込みは `建玉損益（円）`（スワップ除外）を使用
- カラム名「取引区分」→「区分」、「銘柄名」→「通貨ペア」、「売買区分」→「売買」
- ミニ通貨ペア（USM/JPY 等）は標準ペア名（USD/JPY 等）に変換し、lot単位は 1,000 通貨として計算

---

---

### JFX・ヒロセ通商（アプリ版共通フォーマット）

両社は同一の RichFX プラットフォームを使用しており、アプリ版のCSVフォーマットが共通。
共通パーサー `src/main/parsers/jfx.ts` で実装し、口座名・プレフィックスを切り替える。

| 項目 | 内容 |
|------|------|
| **文字コード** | CP932（Shift-JIS互換） |
| **レコード構成** | 決済行のみ（1行1トレード）。新規行は含まれない |
| **ヘッダー構成** | 3行（1行目: フィルタ項目名、2行目: フィルタ値、3行目: 列名） |
| **パーサー** | `src/main/parsers/jfx.ts`（`parseJfxSeriesCsv`） |

**カラム構成（3行目ヘッダー）:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 決済約定日時 | 決済日時 |
| 1 | 注文番号 | 決済注文番号（ID構成に使用） |
| 2 | ポジション番号 | 新規ポジション番号（ID構成に使用） |
| 3 | 通貨ペア | 通貨ペア（例: `USD/JPY`） |
| 9 | 売買 | **クローズ側**の方向（「買」「売」）→ DBの buy_sell は逆方向 |
| 10 | Lot数 | Lot数（1 Lot = 1,000通貨。× 1,000 して登録） |
| 11 | 新規約定日時 | エントリー日時 |
| 12 | 新規約定値 | エントリー価格 |
| 13 | 決済約定値 | 決済価格 |
| 16 | 売買損益 | 確定損益（円）。スワップ除外 |

**スワップの扱い:**
- `スワップ損益`（列18）が別列で管理されており、`決済損益`（列19）= 売買損益 + スワップ損益 の合算値
- パーサーは `売買損益`（列16）を使用するため、スワップを除外した純トレード損益のみ取り込む
- `約定区分 === '決済'` の完全一致フィルタにより、スワップ行が混入しても誤取込は発生しない
- CSVは「決済約定」エクスポートのためスワップ単独行は含まれない（仕様）

**その他注意事項:**
- `売買` 列はクローズ側の方向なので、DBに保存する `buy_sell`（オープン側）は **逆方向** になる
- pip損益の単位は 0.001 刻み（3桁目まで）

---

### ヒロセ通商（Webブラウザ版）

ヒロセ通商のWebブラウザ版（LION FX Web）はアプリ版と異なるフォーマット。
`parseHiroseCsv` が1行目に `決済約定日時` が含まれるかで自動判定し、Webブラウザ版は `parseHiroseWebCsv` に振り分ける。

| 項目 | 内容 |
|------|------|
| **文字コード** | CP932（Shift-JIS互換） |
| **レコード構成** | 決済行・新規行が混在。`約定区分 === '決済約定'` の行のみ取り込む（1行1トレード） |
| **ヘッダー構成** | 1行（列名のみ） |
| **日付フォーマット** | `YY/MM/DD HH:MM:SS`（2桁年）→ `20YY-MM-DDTHH:MM:SS` に変換 |
| **パーサー** | `src/main/parsers/jfx.ts`（`parseHiroseWebCsv`） |

**カラム構成（固定15列）:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 決済約定日時 | 決済日時（新規行は空） |
| 1 | 注文番号 | 決済注文番号（ID構成に使用） |
| 2 | 決済約定値 | 決済価格 |
| 3 | 建玉番号 | ポジション番号（ID構成に使用） |
| 4 | 通貨ペア | 通貨ペア（例: `USD/JPY`） |
| 5 | 約定区分 | 「決済約定」「新規約定」でフィルタ |
| 6 | 売買 | **クローズ側**の方向（「買」「売」）→ DBの buy_sell は逆方向 |
| 7 | Lot数 | Lot数（1 Lot = 1,000通貨。× 1,000 して登録） |
| 8 | 新規約定日時 | エントリー日時 |
| 9 | 新規約定値 | エントリー価格 |
| 10 | 円レート | 円換算レート（未使用） |
| 11 | 売買損益 | 確定損益（円）。スワップ除外 |
| 12 | 手数料 | 手数料（未使用） |
| 13 | スワップ損益 | スワップ損益（未使用） |
| 14 | 決済損益 | スワップ込み合算値（使用しない） |

**アプリ版との主な違い:**
- ヘッダー行数: 1行（アプリ版は3行）
- 約定区分の値: 「決済約定」「新規約定」（アプリ版は「決済」）
- 売買列の方向: クローズ側（アプリ版と同様）→ 逆転が必要
- 日付フォーマット: 2桁年（アプリ版は4桁年）

**重複チェックについて:**
- 同一フォーマット内（Webブラウザ版同士）の重複検出は正常に機能する
- アプリ版とWebブラウザ版で同じトレードを取り込んだ場合、IDの第2要素（建玉番号）がアプリ版のポジション番号と一致しない可能性があるため、クロスフォーマットの重複検出は保証されない

---

### 外為どっとコム

| 項目 | 内容 |
|------|------|
| **文字コード** | CP932（Shift-JIS互換） |
| **レコード構成** | 「約定区分」=「決済」の行のみ取り込む（1行1トレード）。CSVダウンロード時は約定区分「決済」を指定推奨（新規・決済混在でも動作するが、決済のみ出力が最もシンプル） |
| **ヘッダー構成** | 3行（1行目: フィルタ項目名、2行目: フィルタ値、3行目: 列名） |
| **日付フォーマット** | `YY/MM/DD HH:MM:SS`（例: `24/12/18 21:52:14`）→ `20YY-MM-DDTHH:MM:SS` に変換 |
| **パーサー** | `src/main/parsers/gaitame.ts` |

**カラム構成（3行目ヘッダー、JFXと同一レイアウト・列名一部異なる）:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 決済約定日時 | 決済行のみ値あり。空の行は新規行なのでスキップ |
| 1 | 注文番号 | 決済注文番号（ID構成に使用） |
| 2 | ポジション番号 | 新規ポジション番号（ID構成に使用） |
| 3 | 通貨ペア | 通貨ペア |
| 6 | 約定区分 | 「新規」または「決済」でフィルタ |
| 9 | 売買 | **クローズ側**の方向 → DBの buy_sell は逆方向 |
| 10 | Lot数 | Lot数（1 Lot = 1,000通貨。× 1,000 して登録） |
| 11 | 新規約定日時 | エントリー日時 |
| 12 | 新規約定値 | エントリー価格 |
| 13 | 決済約定値 | 決済価格 |
| 19 | 取引損益 | 確定損益（円）。JFXの「決済損益」に相当 |

**スワップの扱い:**
- `スワップ損益`が別列で管理されており、`取引損益`（列19）= スポット損益 + スワップ損益 の合算値
- パーサーは **`スポット損益`** を使用するため、スワップを除外した純トレード損益のみ取り込む
- `約定区分 === '決済'` の完全一致フィルタにより、スワップ行が混在しても誤取込は発生しない

**JFXとの主な違い:**
- 日付フォーマットが `YY/MM/DD` 形式（2桁年）
- 新規・決済行が混在し、「約定区分」列でフィルタが必要
- 各行末に余分なカンマ（空列）あり
- `注文方式`・`スポット損益`・`取引損益` など列名が一部異なる

---

### 楽天 FX

CSVの出力元として **MARKETSPEED FX（ツール）** と **Webブラウザ** の2種類があり、カラム構成が一部異なる。パーサーは両方に対応済み。

| 項目 | 内容 |
|------|------|
| **文字コード** | CP932（Shift-JIS互換）※両方共通 |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル並び順** | 新しい順（降順）→ 決済行が新規行より先に現れるため2パス処理 |
| **ヘッダー構成** | 1行（列名のみ） |
| **パーサー** | `src/main/parsers/rakuten.ts` |

**カラム構成の比較（共通列・差異列）:**

| カラム用途 | ツール版列名 | Web版列名 | 備考 |
|------------|-------------|-----------|------|
| 通貨ペア | `通貨ペア` | `通貨ペア` | 日本語表記 → 標準形式に変換 |
| 注文区分 | `注文区分` | `注文区分` | 「新規」または「決済」 |
| 売買 | `売買` | `売買` | 「買」または「売」 |
| 数量 | `受注数量` | `約定数量` | 通貨数。ツール版はカンマ区切り |
| 約定価格 | `約定レート` | `約定レート` | |
| 損益 | `取引損益(円換算)`（半角括弧） | `取引損益（円換算）`（全角括弧） | 決済行のみ有効。新規行は「-」→ 0 |
| 約定日時 | `約定日時` | `約定日時` | `YYYY/MM/DD HH:MM` 形式 |
| ID | `約定NO.`（大文字） | `約定No.`（小文字） | CSV出力内のシーケンス番号 |
| チャネル | `チャネル`（あり） | なし | 未使用 |
| 取引損益 | なし | `取引損益`（あり） | 未使用 |

パーサーはヘッダー名で列を特定するため、列順序の違いは問題なし。差異のある列名は `??` で両方を試みる。

**通貨ペア変換:**
- CSVは日本語表記（`ドル/円` など）のため、`parserUtils.ts` の `convertCurrencyPair()` で標準形式（`USD/JPY` など）に変換する
- 対応ペア: USD/JPY, EUR/JPY, GBP/JPY, AUD/JPY, NZD/JPY, CAD/JPY, CHF/JPY, ZAR/JPY, TRY/JPY, MXN/JPY, NOK/JPY, SEK/JPY, HUF/JPY, PLN/JPY, CNH/JPY, EUR/USD, GBP/USD, AUD/USD, NZD/USD, USD/CHF, USD/CAD, EUR/GBP, EUR/AUD, EUR/CHF, EUR/NZD, GBP/AUD, GBP/CHF, GBP/NZD, AUD/CAD, AUD/NZD, NZD/CAD, NOK/SEK
- 半角カタカナは `normalizeKana()` で全角に正規化してからマップを引く（松井証券のShift-JISデコード後に半角で来るため）
- 松井証券は通貨名を短縮表記する（`カナダドル` → `カナダ`、`スイスフラン` → `スイス`、`ランド` 等）。両表記をマップに登録済み

**ペアリングロジック:**
- GMO同様、新規/決済の2行ペア方式
- `建単価` 列がないため、**純粋なFIFO照合のみ**
- CSVが降順のため**2パス処理**：第1パスで新規行をすべてキューに収集 → 第2パスで決済行を約定No.昇順にマッチング
- レコードID: `rakuten_{新規約定No}_{決済約定No}`

**日時フォーマット:**
- `YYYY/MM/DD HH:MM`（秒なし）→ `YYYY-MM-DDTHH:MM:00` に変換
- DBには `:00` をプレースホルダーとして格納する（秒数不明のため）
- 画面表示では `:00` を `:--` に置き換えて「秒数不明」であることを明示する（`formatTradeTime()` で制御）
- MT4（`Statement.htm`）は秒数あり。この置換はIDが `rakuten_` 始まりのCSVトレードのみ適用

**保有時間への影響:**
- 秒数がないため、同一分内に完結したトレードの保有時間は `0秒` になる
- `holding_time = 0` のトレードは成績集計の平均保有時間から除外される（`computeStats()` で制御）
- 別の分にまたがるトレードは分単位の近似値として計算される（誤差最大 59 秒）

**スワップの扱い:**
- `取引損益(円換算)` は「売買損益の円換算値」と公式ヘルプで確認済み。スワップは含まれない（純トレード損益）
- 新規行の損益列は「-」のため 0 として扱う

**注意事項:**
- 約定No.はCSV出力ごとのシーケンス番号（同一取引でも異なる出力では番号が変わる場合あり）
- 取込後はCSVが自動削除されるため通常は重複しないが、同じ期間のCSVを複数回エクスポートして取り込む場合は注意

---

### 松井証券

| 項目 | 内容 |
|------|------|
| **文字コード** | Shift-JIS（CP932） |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル並び順** | 新しい順（降順）→ パーサー内で昇順ソート |
| **ヘッダー構成** | 1行（列名のみ） |
| **パーサー** | `src/main/parsers/matsui.ts` |

**カラム構成（全列）:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 通貨ペア | 日本語表記（例: `米ドル/円`）→ 標準形式（`USD/JPY`）に変換 |
| 1 | 売買 | 「買」または「売」（その取引の方向） |
| 2 | 種類 | 「通常」「リピート」など（フィルタには使用しない） |
| 3 | 取引区分 | 「新規」または「決済」 |
| 4 | 数量 | 通貨数（例: `1,000` → 1000）。カンマ除去のみ |
| 5 | 約定価格 | 約定レート |
| 6 | 建玉損益(円) | 純トレード損益（スワップ除外）→ `soneki` として使用 |
| 7 | スワップ | スワップポイント（未使用） |
| 8 | 受渡金額 | 建玉損益 + スワップ の合算値（使用しない） |
| 9 | 受渡日 | 受渡日（未使用） |
| 10 | 約定日時 | `YYYY/MM/DD HH:MM:SS` 形式 → `YYYY-MM-DDTHH:MM:SS` に変換 |
| 11 | 注文日時 | 注文日時（未使用） |

**ペアリングロジック:**
- 注文番号列がないため、約定日時 + 通貨ペア + 数量で一意IDを構成
- 楽天 FX同様の2パス方式：第1パスで新規行をキューに収集 → 第2パスで決済行を時刻昇順にFIFO照合
- レコードID: `matsui_{通貨ペア}_{新規約定日時(数字のみ)}_{決済約定日時(数字のみ)}_{数量}_{新規価格×1000}_{決済価格×1000}`（価格まで含めることでID衝突を実質排除）

**通貨ペア変換:**
- `米ドル/円` → `USD/JPY`（`parserUtils.ts` の `JAPANESE_PAIR_MAP` に追加済み）
- その他のペアは楽天 FXと共通の日本語表記マップで変換

**スワップの扱い:**
- `建玉損益(円)`（列6）= 純トレード損益（スワップ除外）。パーサーはこれを `soneki` として取り込む
- `受渡金額`（列8）= 建玉損益 + スワップ の合算値だが、意図的に使用しない

---

---

### MT4

MT4はFX会社ではなく取引プラットフォームであるため、**既存のブローカーフォルダに `.htm` ファイルを置けば自動的にMT4パーサーで取り込む**設計とする。MT4専用フォルダは作らない。

```
{CSV格納先}/
  FXTF/
    ├── fxtf_export.csv     → FXTFネイティブCSVパーサー（未実装・デフォルト）
    └── Statement.htm       → MT4パーサー（.htm 拡張子で自動判定）
  楽天 FX/
    ├── rakuten_export.csv  → 楽天 FXパーサー
    └── Statement.htm       → MT4パーサー（.htm 拡張子で自動判定）
  OANDA証券/
    └── ReportHistory-*.xlsx → OANDAパーサー（.xlsx + OANDA証券フォルダで自動判定）
```

| 項目 | 内容 |
|------|------|
| **ファイル形式** | HTML（`.htm`）。CSV ではないことに注意 |
| **文字コード** | Shift-JIS |
| **対象ファイル** | `Statement.htm`（「レポートの保存」で出力） |
| **フォルダ** | 各ブローカーの既存フォルダ（例: `FXTF/`, `楽天 FX/`） |
| **レコード構成** | 1行1トレード（決済済みポジションのみ取込） |
| **パーサー** | `src/main/parsers/mt4.ts` |

**取込手順（MT4側の操作）:**
1. MT4を起動する
2. 上部メニューの「表示」→「ターミナル」をクリック（表示済みの場合はスキップ）
3. ターミナルの「口座履歴」タブを選択
4. 期間のカスタム設定を行い、OKをクリック
5. 「口座履歴」の表示上で右クリックし、「レポートの保存」をクリック
6. 保存した `Statement.htm` をブローカーフォルダ（例: `FXTF/`）に配置し、「CSV自動取込」を実行する

**ブローカー別ロット換算係数（要確認）:**

MT4のロット単位はブローカー設定に依存する。`1 lot = X 通貨` の X 値を各ブローカーで確認すること。

| ブローカー | 換算係数 | 確認状況 |
|-----------|---------|---------|
| FXTF MT4 | × 100,000（1 lot = 100,000通貨） | ✅ 確認済み（0.01 lot = 1,000通貨） |
| 楽天 FX MT4 | × 100,000（1 lot = 100,000通貨） | ✅ 確認済み（0.01 lot = 1,000通貨） |

> **確認方法**: MT4のレポートに表示される「サイズ」の値（例: `0.01`）と、実際に取引した通貨数量（例: `1,000通貨`）を照合する。
> 係数が異なる場合は `src/main/parsers/mt4.ts` 内の `MT4_LOT_MULTIPLIERS` マップを更新すること。

**ブローカー別サーバータイムゾーン:**

MT4のStatement.htmに記録される時刻は**ブローカーのサーバー現地時刻**のため、JST（UTC+9）への変換が必要。
変換ロジックは `src/main/parsers/mt4.ts` の `MT4_SERVER_TIMEZONES` マップと `brokerTimeToJst()` で管理する。

| ブローカー | サーバー時刻 | JST変換（冬） | JST変換（夏） | 確認状況 |
|-----------|------------|-------------|-------------|---------|
| FXTF MT4 | GMT+2/+3（EET/EEST） | +7時間 | +6時間 | ✅ 確認済み |
| 楽天 FX MT4 | GMT+2/+3（EET/EEST） | +7時間 | +6時間 | ✅ 確認済み |

欧州夏時間（EEST）の適用期間: **3月最終日曜 03:00 〜 10月最終日曜 04:00**（ブローカー現地時刻）
新しいブローカーを追加する際はサーバー時刻を確認し、`MT4_SERVER_TIMEZONES` に追記すること。

**列構成（14列）:**

| 列番号 | 内容 | 用途 |
|--------|------|------|
| 0 | チケット番号 | レコードID（`mt4_{チケット番号}`）に使用 |
| 1 | 新規日時 | エントリー日時（例: `2026.05.18 04:57:09`） |
| 2 | 種類 | `buy` / `sell` / `balance`。buy/sell のみ取込 |
| 3 | ロット | ロット数（ブローカー別係数で通貨数に変換） |
| 4 | 通貨ペア | ブローカー固有形式（例: `eurusd-cd`）→ `EUR/USD` に変換 |
| 5 | 新規レート | エントリー価格 |
| 8 | 決済日時 | 決済日時。日時形式でない場合はオープンポジション行としてスキップ |
| 9 | 決済レート | 決済価格 |
| 13 | Profit（損益） | 純トレード損益（円）。スワップ除外。`Swap`列（[12]）は別列で管理 |

**通貨ペア変換ロジック:**
- ブローカー付加サフィックスを除去して先頭6文字を取得（`eurusd-cd` → `eurusd`）
- 前3文字 / 後3文字に分割して大文字化し `/` で結合（`EUR/USD`）

**オープンポジションの除外:**
- 決済日時列（[8]）が `YYYY.MM.DD HH:MM:SS` 形式でない行をスキップ
- これによりオープンポジション行・集計行・ヘッダー行を自動除外する

---

### FXTF（ゴールデンウェイ・ジャパン）

| 項目 | 内容 |
|------|------|
| **文字コード** | UTF-8 |
| **レコード構成** | 決済行のみ取り込む（1行1トレード）。決済行に新規情報も含まれる |
| **ヘッダー構成** | 冒頭に会社情報・口座情報の説明文が複数行あり、`【取引明細】` 行の次にヘッダー行が来る |
| **パーサー** | `src/main/parsers/fxtf.ts` |

**ファイル構造:**

ファイルの冒頭に会社情報・口座情報が記載されており、取引データはすぐに来ない。
`【取引明細】` というセクション見出しの次行がヘッダー、その後がデータ行。
`【建玉残高】` 以降はデータ対象外。

**カラム構成:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 取引番号 | 先頭の `'` を除去して使用（Excelエスケープ） |
| 1 | 取引日時（JST） | 決済日時 |
| 3 | 新規/決済 | 「新規」または「決済」でフィルタ |
| 4 | 売or買 | **クローズ側**の方向 → DBの buy_sell は逆方向 |
| 5 | 通貨ペア | `USD/JPY` 形式（変換不要） |
| 6 | 成立価格 | 決済価格 |
| 7 | 数量 | 通貨数（カンマ除去のみ） |
| 9 | 実現損益 | 純トレード損益（スワップ除外）→ `soneki` として使用 |
| 10 | 実現スワップ | スワップ損益（未使用） |
| 13 | 決済対象取引番号 | 新規の取引番号（先頭の `'` を除去）。ID構成に使用 |
| 14 | 決済対象取引日時 | エントリー日時 |
| 15 | 決済対象取引価格 | エントリー価格 |
| 16 | 受渡代金 | 実現損益 + 実現スワップ の合算値（使用しない） |

**レコードID:** `fxtf_{決済対象取引番号}_{取引番号}`

**スワップの扱い:**
- `実現損益`（列9）= 純トレード損益（スワップ除外）。パーサーはこれを `soneki` として取り込む
- `受渡代金`（列16）= 実現損益 + 実現スワップ の合算値だが、意図的に使用しない

**注意事項:**
- 取引番号・決済対象取引番号の先頭に `'` が付いている（ExcelがCSVを開いたときに数値として解釈させないためのエスケープ）。パーサーで除去して使用する
- MT4の `Statement.htm` も同じ `FXTF` フォルダに配置可能。`.htm` 拡張子で自動的にMT4パーサーが使用される

**ダウンロード手順:**
1. https://my.fxtrade.co.jp/portal/login にログイン
2. 上部メニューの「報告書」をクリック
3. 期間（From〜To）を指定し「ダウンロード」をクリック（種別は「取引残高報告書」のまま）
4. 「出力履歴の更新」をクリックし、最新の「ダウンロード」をクリック
5. ※取引当日は出力されないため翌日以降に出力すること。出力可能時間はサイトの注意書きを参照

---

---

### 外貨ex

| 項目 | 内容 |
|------|------|
| **文字コード** | Shift-JIS（CP932） |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル並び順** | 新しい順（降順）。ただし直接参照のためソート不要 |
| **ヘッダー構成** | Webブラウザ版: 1行目がヘッダー / アプリ版: 1行目が「約定履歴,YYYYMMDD」のメタ行、2行目がヘッダー |
| **パーサー** | `src/main/parsers/gaikaex.ts` |

**対応フォーマット:**

2種類のCSVフォーマットに対応。パーサーが自動判別する。

| フォーマット | 1行目 | 通貨ペア表記 |
|------------|-------|------------|
| Webブラウザ版 | ヘッダー行 | 日本語（例: `ドル/円`）→ 標準形式に変換 |
| アプリ版（外貨ex for Windows） | `約定履歴,YYYYMMDD` | `USD/JPY` 形式（変換不要） |

**Webブラウザ版カラム構成:**

| カラム名 | 用途 |
|----------|------|
| 注文番号 | 注文ID |
| 新規決済 | 「新規」または「決済」 |
| 通貨ペア | 日本語表記（`convertCurrencyPair()` で変換） |
| 売買 | 「買」または「売」 |
| 約定価格 | 約定価格 |
| 注文数量 | 通貨数 |
| 約定日時 | エントリー/決済日時 |
| 新規注文番号 | 対応する新規行の注文番号（決済行のみ） |
| 新規約定価格 | エントリー価格（決済行のみ） |
| 売買損益 | 純トレード損益（円）（決済行のみ） |

**アプリ版カラム構成:**

| カラム名 | 用途 |
|----------|------|
| 注文番号 | 注文ID |
| 通貨ペア | `USD/JPY` 形式 |
| 売買 | 「買」または「売」 |
| 新規決済 | 「新規」または「決済」 |
| 注文数量 | 通貨数 |
| 約定価格 | 約定価格 |
| 約定日時 | エントリー/決済日時 |
| 売買損益 | 純トレード損益（円）（決済行のみ） |
| 新規注文番号 | 対応する新規行の注文番号（決済行のみ） |
| 新規約定価格 | エントリー価格（決済行のみ） |

**ペアリングロジック:**
- 新規行を `注文番号 → 行情報` のマップに格納
- 決済行の `新規注文番号` を使って直接参照する（キュー方式不要）
- レコードID: `gaikaex_{新規注文番号}_{決済注文番号}`

**スワップの扱い:**
- `売買損益` は純トレード損益のみ（スワップ除外）。パーサーはこれを `soneki` として取り込む
- アプリ版の `スワップ累計` 列は意図的に使用しない

---

### LINE FX

| 項目 | 内容 |
|------|------|
| **文字コード** | UTF-8 |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル並び順** | 新しい順（降順）→ パーサー内で昇順ソートしてFIFO照合 |
| **ヘッダー構成** | 1〜3行目: サマリー行（空行含む）。4行目: データヘッダー。5行目以降: データ行 |
| **パーサー** | `src/main/parsers/linefx.ts` |

**ファイル構造:**

```
"数量合計×10,000","建玉損益合計(円)","スワップ合計(円)","決済損益合計(円)"
"0.4","5","0","5"
                          ← 空行（parseCsvLines でスキップ）
"通貨ペア","売買","取引区分","数量x10,000","約定レート","建玉損益(円)","累計スワップ(円)","決済損益(円)","注文日時","約定日時","取引番号"
"USD/JPY","売","決済","0.1","158.944","+1","0","+1","2026/05/19 12:37:09","2026/05/19 12:37:09","2613900003620302"
"USD/JPY","買","新規","0.1","158.940","-","-","-","2026/05/19 12:36:21","2026/05/19 12:36:21","2613900003619002"
```

**カラム構成:**

| カラム名 | 用途 |
|----------|------|
| 通貨ペア | `USD/JPY` 形式（変換不要） |
| 売買 | 「買」または「売」（その行の売買方向） |
| 取引区分 | 「新規」または「決済」 |
| 数量x10,000 | 万通貨単位（例: 0.1 = 1,000通貨）。× 10,000 して通貨数に変換 |
| 約定レート | 約定価格 |
| 決済損益(円) | 決済行のみ有効。新規行は「-」 |
| 約定日時 | エントリー/決済日時（`YYYY/MM/DD HH:MM:SS` 形式） |
| 取引番号 | 注文ID（重複チェックとペアリングに使用） |

**ペアリングロジック:**
- 外貨exのような「新規注文番号」列がないため、**同一通貨ペア × 反対売買の組み合わせでFIFO照合**
- 新規行を `{通貨ペア}_{売買}` キーのキューに積み、約定日時昇順にソート
- 決済行を約定日時昇順にソートし、`決済売 → 新規買キュー`・`決済買 → 新規売キュー` から FIFO で dequeue
- レコードID: `linefx_{新規取引番号}_{決済取引番号}`

**スワップの扱い:**
- `決済損益(円)` = 建玉損益 + 累計スワップ の合算値
- サンプルデータではスワップが0のため合算値と純損益が一致しているが、スワップが発生した場合はスワップ込みの値が `soneki` に格納される点に注意
- 現時点ではスワップを分離できる列が存在しないため、この仕様とする

---

---

### みんなのFX（トレイダーズ証券）

出力元が「WEBトレーダー」と「FXトレーダー」の2種類あり、列数でパーサーが自動判定する。

| 項目 | 内容 |
|------|------|
| **文字コード** | Shift-JIS（CP932） |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル並び順** | 新しい順（降順）。決済行の損益対応約定番号で直接参照するためソート不要 |
| **ヘッダー構成** | 1行（列名のみ） |
| **パーサー** | `src/main/parsers/minnafx.ts` |

**フォーマット自動判定:**
- 12列 → WEBトレーダー
- 14列 → FXトレーダー

**WEBトレーダー カラム構成（12列）:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 通貨ペア | `USDJPY LIGHT` 形式 → `USD/JPY` に変換 |
| 1 | 区分 | 「新規」または「決済」 |
| 2 | 売買 | 「買」または「売」（ポジション方向。新規・決済とも同値） |
| 3 | 数量 | 万通貨単位（例: `0.1` = 1,000通貨）。× 10,000 して登録 |
| 4 | 約定価格 | 約定レート |
| 5 | 建玉損益 | 純トレード損益（円）。スワップ除外 → `soneki` として使用 |
| 6 | 累計スワップ | スワップ損益（未使用） |
| 7 | 手数料 | 手数料（未使用） |
| 8 | 決済損益 | 建玉損益 + 累計スワップ の合算値（使用しない） |
| 9 | 約定日時 | `YYYY-MM-DD HH:MM:SS` 形式 → `YYYY-MM-DDTHH:MM:SS` に変換 |
| 10 | 取引番号 | 注文ID（ペアリングに使用） |
| 11 | 決済対象取引番号 | 決済行のみ：対応する新規行の取引番号 |

**FXトレーダー カラム構成（14列）:**

WEBトレーダーに `Pips損益`（列5）・`円転レート`（列6）が追加され、以降の列番号が2ずつずれる。

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 5 | Pips損益 | （未使用） |
| 6 | 円転レート | （未使用） |
| 7 | 建玉損益 | 純トレード損益（円）。スワップ除外 → `soneki` として使用 |
| 8 | 累計スワップ | スワップ損益（未使用） |
| 9 | 手数料 | 手数料（未使用） |
| 10 | 決済損益 | 建玉損益 + 累計スワップ の合算値（使用しない） |
| 11 | 約定日時 | エントリー/決済日時 |
| 12 | 取引番号 | 注文ID（ペアリングに使用） |
| 13 | 決済対象取引番号 | 決済行のみ：対応する新規行の取引番号 |

**通貨ペア変換:**
- `USDJPY LIGHT` → 末尾の ` LIGHT` を除去し先頭3文字+`/`+後続3文字 → `USD/JPY`

**ペアリングロジック:**
- 新規行を `取引番号 → 行データ` のマップに収集
- 決済行の `決済対象取引番号` でマップを直接参照（外貨ex Webブラウザ版と同方式）
- レコードID: `minnafx_{新規取引番号}_{決済取引番号}`

**スワップの扱い:**
- `建玉損益`（WEB列5 / FX列7）= 純トレード損益（スワップ除外）→ `soneki` として使用
- `累計スワップ`（WEB列6 / FX列8）が別列で管理されており意図的に使用しない
- `決済損益`（WEB列8 / FX列10）= 建玉損益 + 累計スワップ の合算値（使用しない）

**価格精度:**
- クロス円通貨ペア（EUR/JPY、GBP/JPY 等）の約定価格は CSV に小数点4桁目まで格納されているが、画面表示は3桁目まで（通常のクロス円表示）。

---

### LIGHT FX（トレイダーズ証券）

みんなのFXと同じトレイダーズ証券が提供するプラットフォームのため、CSVフォーマットはみんなのFXと完全に同一。
出力元が「シンプルトレーダー」と「アドバンスドトレーダー」の2種類あり、列数でパーサーが自動判定する。

| 項目 | 内容 |
|------|------|
| **文字コード** | Shift-JIS（CP932） |
| **レコード構成** | 新規行 + 決済行 の2行で1トレード |
| **ファイル名** | `execution.YYMMDD.HHMMSS.csv` 形式 |
| **パーサー** | `src/main/parsers/lightfx.ts` |

**フォーマット自動判定:**
- 12列 → シンプルトレーダー（みんなのFX WEBトレーダー相当）
- 14列 → アドバンスドトレーダー（みんなのFX FXトレーダー相当）

カラム構成・ペアリングロジック・通貨ペア変換はみんなのFXと完全に同じ。レコードIDのプレフィックスは `lightfx_`。

**価格精度:**
- クロス円通貨ペア（EUR/JPY、GBP/JPY 等）の約定価格は CSV に小数点4桁目まで格納されているが、画面表示は3桁目まで（通常のクロス円表示）。

---

---

### OANDA証券（MT5）

| 項目 | 内容 |
|------|------|
| **ファイル形式** | XLSX（`.xlsx`）。Open XML形式 |
| **文字コード** | UTF-16 LE（zipファイル内のXMLがUTF-16 LE） |
| **対象ファイル** | `ReportHistory-XXXXXXXXX.xlsx`（「レポート」→「Open XML(MS Office Excel 2007)」で出力） |
| **フォルダ** | `OANDA証券/` |
| **レコード構成** | 1行1ポジション（決済済みのみ取込） |
| **パーサー** | `src/main/parsers/oanda.ts` |

**取込手順（MT5側の操作）:**
1. MT5にログイン
2. 上部メニュー「表示」から「ツールボックス」をクリック
3. 「口座履歴」タブを選択する
4. 右クリックして期間指定(From～To)を行い、OKをクリック
5. 右クリックして「レポート」から「Open XML(MS Office Excel 2007)」を選択
6. 保存した `.xlsx` ファイルを `OANDA証券/` フォルダに配置して取り込む

> HTMLの「Internet Explorer」形式は画像ファイルも同時に生成されるため使用しない。

**列構成（ポジション一覧セクション）:**

| 列 | 内容 | 用途 |
|----|------|------|
| A（index 0） | エントリー日時 | `YYYY.MM.DD HH:MM:SS` 形式 |
| B（index 1） | ポジション番号 | レコードID（`oanda_{ポジション番号}`）に使用 |
| C（index 2） | 通貨ペア | `USDJPY.cl` 形式 → `.cl` 除去して `USD/JPY` に変換 |
| D（index 3） | タイプ | `buy` / `sell` |
| E（index 4） | ロット | ロット数（× 100,000 して通貨数に変換） |
| F（index 5） | エントリー価格 | |
| I（index 8） | 決済日時 | 決済済み確認に使用 |
| J（index 9） | 決済価格 | |
| K（index 10） | 手数料 | （未使用） |
| L（index 11） | スワップ | スワップ除去のために使用 |
| M（index 12） | 合計 | スワップ込み損益。`soneki = M - L` で純損益を算出 |

**ポジション一覧行の識別条件:**
- D列（タイプ）が `buy` または `sell`
- E列（ロット）にスラッシュ（`/`）が含まれない（「決済履歴」セクションの行を除外）
- I列（決済日時）が `YYYY.MM.DD HH:MM:SS` 形式（決済済みポジションのみ）

**サーバータイムゾーン:**
- GMT+2（冬時間）/ GMT+3（欧州夏時間）→ JSTは +7h/+6h（MT4と同一設定）
- GMT+2/+3（欧州夏時間）✅ 確認済み

---

### SBI FX

**取得方法:** Rich Client NEXT → 照会 → 約定履歴 → CSVアイコン

**ファイル:** `sbi.csv`（Shift-JIS + ヌルバイト混入）

**カラム構成（2行目がヘッダー、1行目はタイトル行）:**

| 列 | カラム名 | 内容 |
|---|---|---|
| 0 | 約定日時 | `YYYY/MM/DD HH:MM:SS` |
| 1 | 通貨ペア | `USD/JPY` 等 |
| 2 | 注文区分 | 通常 等 |
| 3 | 売買 | 買 / 売 |
| 4 | 新規決済 | 新規 / 決済 |
| 5 | 執行条件 | 成行 等 |
| 6 | 約定価格 | エントリー価格（新規）/ 決済価格（決済） |
| 7 | 約定数量 | 通貨単位（例: 1000 = 1Kロット） |
| 8 | 約定通貨 | USD 等 |
| 9 | 決済相手約定価格 | 対応新規の約定価格（決済行のみ）← マッチングキー |
| 10 | 損益PIPS | pips |
| 11 | 実現損益 | 損益（円）小数あり（例: 10.2000） |
| 12 | ｽﾜｯﾌﾟﾎﾟｲﾝﾄ | スワップポイント |
| 13 | 決済通貨 | JPY 等 |
| 14 | 注文番号 | 注文ID |
| 15 | 備考 | - |

**パース戦略:**
- 新規・決済の2行で1トレードを構成（GMOと同様）
- `決済相手約定価格` = 対応する新規の `約定価格` でマッチング
- UID: `sbi_{新規注文番号}_{決済注文番号}`
- エンコード: Shift-JIS + ヌルバイト（`0x00`）混入 → `ipc.ts`でデコード後にヌルバイト除去

---

---

## 先物取引パーサー

先物取引の CSV パーサーは FX パーサーとは独立しており、`futures_trades` テーブルに格納する。
`trade_history` テーブルとは無関係のため、スワップポリシー・Lot単位などの FX 共通仕様は適用されない。

---

### 松井証券先物

| 項目 | 内容 |
|------|------|
| **文字コード** | Shift-JIS（CP932） |
| **レコード構成** | 新規行 + 返済行 の2行で1トレード |
| **ファイル並び順** | 不定（パーサー内で時刻昇順ソートして FIFO 保証） |
| **ヘッダー構成** | 1行（列名のみ） |
| **ファイル配置先** | `{CSV格納先}/松井証券先物/futoporder_*.csv` |
| **パーサー** | `src/main/parsers/futuresMatsui.ts` |

**カラム構成（全18列）:**

| 列番号 | カラム名 | 用途 |
|--------|----------|------|
| 0 | 状態 | `約定済` の行のみ取り込む |
| 1 | 銘柄 | 銘柄名（例: `日経225mini26年09月限`）。コントラクト種別判定に使用 |
| 2 | 取引区分 | `新規*` または `返済*`（前方一致）でフィルタ |
| 3 | 条件 | 未使用 |
| 4 | 売買 | 「買」または「売」（その行の方向） |
| 5 | 発注数 | 枚数（`lot` として格納） |
| 6 | 値段 | 未使用（指値価格） |
| 7 | トリガー | 未使用 |
| 8 | 予約値段 | 未使用 |
| 9 | 受付日時 | `YY/MM/DD HH:MM` 形式。日付部分を取得するために使用 |
| 10〜14 | 各種設定 | 未使用 |
| 15 | 約定数 | 未使用 |
| 16 | 約定単価 | 約定価格（カンマ除去して `parseFloat`） |
| 17 | 約定時間 | `HH:MM` 形式。受付日時の日付と組み合わせて `YYYY-MM-DDTHH:MM:00` を生成 |

**コントラクト種別判定（銘柄名から自動判定）:**

| 条件 | contract_type | pt_value |
|------|-------------|----------|
| `mini`（英小文字）を含む | `mini` | 100円/pt |
| `micro` または `マイクロ` を含む | `micro` | 10円/pt |
| 上記以外 | `large` | 1,000円/pt |

**ペアリングロジック:**
- 第1パス: `取引区分 = 新規*` の行を `{銘柄}_{売買}` キーのキューに積む
- キューを `受付日時 + 約定時間` で昇順ソートして FIFO を保証（同時刻の場合は価格順）
- 第2パス: `取引区分 = 返済*` の行を時刻昇順で処理。返済の売買方向の逆が新規のキー（返済:売 → キー:買）
- `buy_sell` には**新規側の売買方向**を格納（返済行の方向ではない）

**損益計算:**
- `pnl_pt = (exit_price - entry_price) × direction`（買=+1, 売=−1）
- `pnl_yen = round(pnl_pt × pt_value × lot)`

**保有時間:**
- 秒数がないため分単位で計算
- 同一分（または退場が早い）→ 60秒
- 異なる分 → `(exit_min - entry_min) × 60` 秒

**セッション判定:**
- 日中（`daytime`）: エントリー時刻が 08:45〜15:30 の範囲内
- 夜間（`night`）: それ以外

**レコードID:**
```
futures_matsui_{銘柄正規化}_{エントリー日時数字列}_{イグジット日時数字列}_{枚数}_{エントリー価格整数}_{イグジット価格整数}
```
- 銘柄正規化: 英数字・ひらがな・カタカナ・漢字以外を `_` に置換
- 例: `futures_matsui_日経225mini26年09月限_202606120904_202606120906_1_38500_38510`
- 同一ID衝突時はサフィックス `_2`, `_3` ... を付与

---

## 未実装口座（将来の実装参考）

以下の口座はフォルダ検索対象に含まれているが、CSVパーサーは未実装。
取り込み時に「デフォルトパース（汎用）」が適用されるが、フォーマットが合わない場合は専用パーサーが必要。

---

## パーサー実装時のチェックリスト

新しい口座のパーサーを実装する際に確認すること：

- [ ] **文字コード**: UTF-8 / Shift-JIS / CP932 のどれか → `shiftJisAccounts`（`ipc.ts`）への追加要否
- [ ] **レコード構成**: 1行1トレード（決済行のみ）か、新規+決済の2行ペアか
- [ ] **ファイル並び順**: 昇順か降順か → 降順の場合はパーサー内でソートが必要
- [ ] **ペアリング方法**: 建単価で照合可能か、FIFOのみか
- [ ] **損益カラム**: スワップ込みの合算値か、別列か
- [ ] **通貨ペアの表記**: `USD/JPY` 形式か、`USDJPY` 形式かなど
- [ ] **レコードIDの設計**: 重複が発生しない一意なID構成を検討する
- [ ] **実装ファイル**: `src/main/parsers/{口座名}.ts` を作成
- [ ] **IPC登録**: `ipc.ts` に `csv:parse{口座名}` ハンドラーを追加
- [ ] **Preload登録**: `preload/index.ts` と `index.d.ts` に API を追加
- [ ] **フロントエンド**: `TradeHistoryList.tsx` の取り込み分岐に追加
